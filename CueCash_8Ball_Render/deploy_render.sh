#!/bin/bash
echo "Helper script for deploying CueCash on Render."
echo "In Render's shell, you can run:"
echo "  cd backend && npm install && node server.js &"
echo "Then serve the frontend directory with a static server if needed."
